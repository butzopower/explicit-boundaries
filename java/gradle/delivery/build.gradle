group 'com.example.catalog.delivery'
version '1.0-SNAPSHOT'

apply plugin: 'application'

dependencies {
    implementation project(path: ":core")
    implementation project(path: ":adapter")
}

application {
    mainClass = 'com.example.catalog.delivery.CLI'
}